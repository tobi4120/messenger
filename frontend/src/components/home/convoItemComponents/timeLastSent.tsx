import React from "react"
import { message } from "../../interfaces";

interface props {
    lastMessage: message
}

const TimeLastSent: React.FC<props> = (props) => {
    return (
        <div>
        </div>
    )
}
export default TimeLastSent